# ===================================================================
# LOCAL SPRING BOOT PROPERTIES
# ===================================================================

spring:
  datasource:
    driver-class-name: net.sf.log4jdbc.sql.jdbcapi.DriverSpy
    url: jdbc:log4jdbc:mariadb://172.30.1.11:3306/MINI_BOARD?characterEncoding=UTF-8
    hikari:
      username: root
      password: root
      maximum-pool-size: 3

server:
  port: 8080

logging:
  config: classpath:logback/logback-local.xml
